server:
  port: 8044
  servlet:
    contextPath: /api

spring:
  messaging:
    host: localhost
    password: guest
    port: 5672
    username: guest
    exchange: user.exchange
    queue: user.queue
    routingkey: user.routingkey